# Increase daemon heap so compilation-heavy test suites (E2E, regression baseline)
# don't crash on HPC nodes where the default -Xmx512m is too small.
org.gradle.jvmargs=-Xms512m -Xmx2g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
