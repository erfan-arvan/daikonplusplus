{
  "invariants" : [ {
    "expression" : "this.minCheckCount == minCheckCount",
    "rationale" : "The method directly assigns the parameter to the object's minCheckCount field, so they are equal at method exit.",
    "meta" : [ ]
  } ]
}