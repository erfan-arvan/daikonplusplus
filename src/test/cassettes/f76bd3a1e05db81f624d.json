{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method constructs and returns a new JavaHoodieBackedTableMetadataWriter, which should not return null.",
    "meta" : [ ]
  }, {
    "expression" : "result instanceof JavaHoodieBackedTableMetadataWriter",
    "rationale" : "The returned object is created with 'new JavaHoodieBackedTableMetadataWriter', so it should be of that type.",
    "meta" : [ ]
  }, {
    "expression" : "result.getWriteConfig() == writeConfig",
    "rationale" : "Assuming JavaHoodieBackedTableMetadataWriter stores and exposes the passed writeConfig via getWriteConfig().",
    "meta" : [ ]
  }, {
    "expression" : "result.getContext() == context",
    "rationale" : "Assuming the context parameter is stored in the result and retrievable.",
    "meta" : [ ]
  }, {
    "expression" : "result.getInflightInstantTimestamp().equals(inflightInstantTimestamp)",
    "rationale" : "If result exposes inflightInstantTimestamp via a getter, it should be equal to the input parameter.",
    "meta" : [ ]
  } ]
}