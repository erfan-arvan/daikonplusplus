{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "Since the method always throws an exception and never returns normally, 'result' will always be null at the method exit if this point is reached (by Java semantics, only in case of abnormal exit).",
    "meta" : [ ]
  } ]
}