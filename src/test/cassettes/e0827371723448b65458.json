{
  "invariants" : [ {
    "expression" : "(name == null && result == -1)",
    "rationale" : "If name is null, method returns -1, so result must be -1.",
    "meta" : [ {
      "key" : "nullCheck",
      "value" : "guards result when name is null"
    } ]
  }, {
    "expression" : "(name != null && result == x + name.length())",
    "rationale" : "If name is not null, result is sum of x and length of name.",
    "meta" : [ {
      "key" : "resultDefinition",
      "value" : "exact calculation of result when name is not null"
    } ]
  }, {
    "expression" : "(result >= -1)",
    "rationale" : "Result is either -1 or sum of x and name length; sum cannot be less than -1 since name.length() >= 0.",
    "meta" : [ {
      "key" : "range",
      "value" : "result lower bound due to method logic"
    } ]
  }, {
    "expression" : "(name == null || result >= x)",
    "rationale" : "Because name.length() >= 0, result must be at least x if name is not null.",
    "meta" : [ {
      "key" : "rangeRelation",
      "value" : "result compared to x when name not null"
    } ]
  } ]
}