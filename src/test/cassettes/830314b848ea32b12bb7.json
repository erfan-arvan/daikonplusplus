{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method always returns a non-null lambda, so result is non-null.",
    "meta" : [ ]
  }, {
    "expression" : "result.get() == 0",
    "rationale" : "The method returns () -> 0, so invoking get() on result always yields 0.",
    "meta" : [ ]
  } ]
}