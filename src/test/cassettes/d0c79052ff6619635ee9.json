{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "Assuming that getLabel always returns a non-null Label instance, this invariant ensures that the result is never null.",
    "meta" : [ ]
  } ]
}