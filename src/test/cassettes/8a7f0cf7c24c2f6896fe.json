{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "Since this method always throws an exception and never returns normally, the result is always null at the exit point.",
    "meta" : [ ]
  } ]
}