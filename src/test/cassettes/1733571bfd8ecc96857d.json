{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "Since the method always throws an exception and never returns, 'result' can never exist; if it were to exist, it would be null.",
    "meta" : [ ]
  } ]
}