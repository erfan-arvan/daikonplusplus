{
  "invariants" : [ {
    "expression" : "result == HoodieRecord.HoodieRecordType.AVRO",
    "rationale" : "The method implementation always returns HoodieRecord.HoodieRecordType.AVRO, so this must be true at the method exit.",
    "meta" : [ ]
  } ]
}