{
  "invariants" : [ {
    "expression" : "result == HoodieRecord.HoodieRecordType.AVRO",
    "rationale" : "The method implementation always returns HoodieRecord.HoodieRecordType.AVRO, so result must equal that value at all exits.",
    "meta" : [ ]
  } ]
}