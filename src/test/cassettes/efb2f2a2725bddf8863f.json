{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "The method always throws an exception, so no non-trivial property can be guaranteed except tautologies.",
    "meta" : [ {
      "key" : "trivial",
      "value" : "The body always throws; no state is changed and the program cannot return normally."
    } ]
  } ]
}