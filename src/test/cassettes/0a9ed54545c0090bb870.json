{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "Since the method simply returns the 'selection' field, which is likely initialized and managed by the Tree class, it's reasonable to expect it to be non-null at method exit in most implementations.",
    "meta" : [ ]
  } ]
}