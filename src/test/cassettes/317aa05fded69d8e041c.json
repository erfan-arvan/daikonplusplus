{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method body always throws before returning, so the result is never assigned and must be null.",
    "meta" : [ ]
  } ]
}