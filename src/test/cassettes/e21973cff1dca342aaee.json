{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "arePartitionRecordsSorted always returns true regardless of object state.",
    "meta" : [ {
      "key" : "justification",
      "value" : "Method is trivial and always returns true, so the contract is always satisfied."
    } ]
  } ]
}