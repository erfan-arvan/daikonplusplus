{
  "invariants" : [ {
    "expression" : "this.map == map",
    "rationale" : "The method assigns the parameter 'map' to the field 'this.map', so at exit they must reference the same object.",
    "meta" : [ ]
  } ]
}