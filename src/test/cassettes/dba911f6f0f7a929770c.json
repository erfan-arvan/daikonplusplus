{
  "invariants" : [ {
    "expression" : "context != null",
    "rationale" : "The method requires a valid HoodieEngineContext for operation; likely always non-null.",
    "meta" : [ ]
  }, {
    "expression" : "instantTime != null",
    "rationale" : "An instant time is needed to coordinate commit operations; it should not be null.",
    "meta" : [ ]
  }, {
    "expression" : "preppedRecords != null",
    "rationale" : "The method is designed to process deletion for prepared records, so the list is probably not null.",
    "meta" : [ ]
  }, {
    "expression" : "preppedRecords.isEmpty() || preppedRecords.size() >= 0",
    "rationale" : "List.size() is always non-negative; includes the case when the list is empty.",
    "meta" : [ ]
  }, {
    "expression" : "!preppedRecords.isEmpty() || preppedRecords.size() == 0",
    "rationale" : "If the list is empty, then its size is exactly zero.",
    "meta" : [ ]
  } ]
}