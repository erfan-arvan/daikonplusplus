{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method always returns SINGLETON_INSTANCE, which should be initialized to a valid object.",
    "meta" : [ ]
  }, {
    "expression" : "result == JavaUpgradeDowngradeHelper.SINGLETON_INSTANCE",
    "rationale" : "The method returns SINGLETON_INSTANCE directly, so the result will always refer to that object.",
    "meta" : [ ]
  } ]
}