{
  "invariants" : [ {
    "expression" : "result == maxHeight.get(actor)",
    "rationale" : "The method returns the value of maxHeight.get(actor), so the result must always equal this expression.",
    "meta" : [ ]
  } ]
}