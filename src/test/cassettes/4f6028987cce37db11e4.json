{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method always throws an exception and never returns normally, so any observed result must be null.",
    "meta" : [ ]
  } ]
}