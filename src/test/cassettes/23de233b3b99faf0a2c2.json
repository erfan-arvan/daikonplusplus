{
  "invariants" : [ {
    "expression" : "result >= -1",
    "rationale" : "The result is either -1 if name is null or x + name.length() otherwise, hence result is >= -1.",
    "meta" : [ {
      "key" : "rationale",
      "value" : "Because -1 is returned only if name is null, otherwise result is sum of x and length of name, which is >= 0."
    } ]
  }, {
    "expression" : "result == -1 || (name != null && result == x + name.length())",
    "rationale" : "At method exit, either name is null and result is -1, or result equals x + name.length().",
    "meta" : [ {
      "key" : "rationale",
      "value" : "Reflects method logic; if result not -1 then equals x + name.length()."
    } ]
  }, {
    "expression" : "name == null || name.length() >= 0",
    "rationale" : "If name is non-null, its length is non-negative.",
    "meta" : [ {
      "key" : "rationale",
      "value" : "String length is always >= 0 for non-null string."
    } ]
  }, {
    "expression" : "result != -1 || name == null",
    "rationale" : "The method returns -1 only when name is null.",
    "meta" : [ {
      "key" : "rationale",
      "value" : "If result equals -1 then name must be null."
    } ]
  }, {
    "expression" : "result != -1 || (name != null) == false",
    "rationale" : "This is equivalent to the previous invariant enforcing the null check when result is -1.",
    "meta" : [ {
      "key" : "rationale",
      "value" : "If result is -1, then name is definitely null."
    } ]
  } ]
}