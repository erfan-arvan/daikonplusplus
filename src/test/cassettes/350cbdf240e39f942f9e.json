{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method always throws an exception and never returns normally, so 'result' must always be null at the exit point.",
    "meta" : [ ]
  }, {
    "expression" : "true == false",
    "rationale" : "No invariant over program variables can be true at this program point because method never completes normally (always throws exception).",
    "meta" : [ ]
  } ]
}