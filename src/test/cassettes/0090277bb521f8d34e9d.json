{
  "invariants" : [ {
    "expression" : "this.maxLength == maxLength",
    "rationale" : "The method assigns the parameter maxLength to the field this.maxLength, so at method exit they must be equal.",
    "meta" : [ ]
  } ]
}