{
  "invariants" : [ {
    "expression" : "result == maxWidth",
    "rationale" : "The method directly returns the value of the maxWidth field, so the returned result must always equal maxWidth at the exit point.",
    "meta" : [ ]
  } ]
}