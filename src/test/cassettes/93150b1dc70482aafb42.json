{
  "invariants" : [ {
    "expression" : "result == records",
    "rationale" : "The method always returns 'records', as shown by both return statements.",
    "meta" : [ ]
  }, {
    "expression" : "result.isEmpty() == records.isEmpty()",
    "rationale" : "The returned list's emptiness matches the input list's emptiness since the reference is either returned directly or after sort.",
    "meta" : [ ]
  } ]
}