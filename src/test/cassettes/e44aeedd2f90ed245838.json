{
  "invariants" : [ {
    "expression" : "args != null && args.length >= 0",
    "rationale" : "The String[] args parameter to main is guaranteed to be non-null, per the Java specification.",
    "meta" : [ {
      "key" : "rationale",
      "value" : "The main method args array is never null and has zero or more elements."
    } ]
  } ]
}