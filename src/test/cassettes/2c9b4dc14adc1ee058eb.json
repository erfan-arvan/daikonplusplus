{
  "invariants" : [ {
    "expression" : "result == prefWidth",
    "rationale" : "The method returns the field 'prefWidth' directly, so the result must be equal to prefWidth.",
    "meta" : [ ]
  }, {
    "expression" : "result == null || result instanceof Value",
    "rationale" : "If result is not null, it must be an instance of Value, because the return type is Value.",
    "meta" : [ ]
  } ]
}