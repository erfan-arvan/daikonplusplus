{
  "invariants" : [ {
    "expression" : "result == writeMetadata",
    "rationale" : "The method returns the input parameter directly, so the output must be exactly the input.",
    "meta" : [ ]
  } ]
}