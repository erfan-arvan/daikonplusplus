{
  "invariants" : [ {
    "expression" : "sortMode != null",
    "rationale" : "The method immediately accesses sortMode in a switch, which will throw NullPointerException if sortMode is null.",
    "meta" : [ ]
  }, {
    "expression" : "sortMode == BulkInsertSortMode.NONE || sortMode == BulkInsertSortMode.GLOBAL_SORT",
    "rationale" : "Only these two cases are handled directly; any other value results in an exception.",
    "meta" : [ ]
  } ]
}