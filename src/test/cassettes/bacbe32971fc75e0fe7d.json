{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method always throws an exception and never returns a value, so result must be null at the exit point.",
    "meta" : [ ]
  } ]
}