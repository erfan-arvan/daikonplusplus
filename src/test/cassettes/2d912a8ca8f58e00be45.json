{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "No meaningful invariants can be inferred from a single boolean parameter at the method entry, so the default always-true invariant is included for completeness.",
    "meta" : [ ]
  } ]
}