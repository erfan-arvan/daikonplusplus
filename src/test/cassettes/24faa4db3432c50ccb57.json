{
  "invariants" : [ {
    "expression" : "tile != null",
    "rationale" : "The method accesses tile fields and methods; tile must be non-null for correctness.",
    "meta" : [ ]
  }, {
    "expression" : "tileElement != null",
    "rationale" : "Method accesses methods on tileElement, thus must be non-null at entry.",
    "meta" : [ ]
  }, {
    "expression" : "tile.getProperties() != null",
    "rationale" : "Method stores values into tile.getProperties(), so must not be null for operations to succeed.",
    "meta" : [ ]
  }, {
    "expression" : "tileElement.isObject()",
    "rationale" : "JsonValue tileElement is being used as an object mapping property names, so likely represents a JSON object.",
    "meta" : [ ]
  }, {
    "expression" : "tileElement.has(\"terrain\") || tileElement.has(\"probability\") || tileElement.has(\"type\") || tileElement.has(\"properties\")",
    "rationale" : "The method retrieves these properties, so at least one is likely present for a non-trivial invocation.",
    "meta" : [ ]
  } ]
}