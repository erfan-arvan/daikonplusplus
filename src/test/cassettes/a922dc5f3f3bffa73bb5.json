{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The execute() method is expected to return a valid HoodieWriteMetadata object.",
    "meta" : [ ]
  }, {
    "expression" : "result.getClass() == HoodieWriteMetadata.class || HoodieWriteMetadata.class.isAssignableFrom(result.getClass())",
    "rationale" : "The return type should align with HoodieWriteMetadata.",
    "meta" : [ ]
  }, {
    "expression" : "result.getWriteStatuses() != null",
    "rationale" : "Since result is constructed via write(), its internal status list should not be null.",
    "meta" : [ ]
  }, {
    "expression" : "result.getWriteStatuses() instanceof List",
    "rationale" : "The getWriteStatuses() method should yield a List, matching the method's generic signature.",
    "meta" : [ ]
  }, {
    "expression" : "result.getWriteStatuses().size() >= 0",
    "rationale" : "A List's size cannot be negative.",
    "meta" : [ ]
  } ]
}