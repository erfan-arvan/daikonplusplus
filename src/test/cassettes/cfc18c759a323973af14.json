{
  "invariants" : [ {
    "expression" : "result == maxWidth.get(actor)",
    "rationale" : "The method consists of a single return statement, so the result always equals the value returned by 'maxWidth.get(actor)'.",
    "meta" : [ ]
  } ]
}