{
  "invariants" : [ {
    "expression" : "args != null",
    "rationale" : "The Java main method expects args to be a non-null array, as per the Java specification.",
    "meta" : [ ]
  }, {
    "expression" : "args.length >= 0",
    "rationale" : "Array length in Java is always at least 0, so this always holds.",
    "meta" : [ ]
  } ]
}