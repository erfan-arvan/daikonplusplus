{
  "invariants" : [ {
    "expression" : "false",
    "rationale" : "The method unconditionally throws an exception, so control never reaches the exit point; the only possible invariant is that this point is unreachable.",
    "meta" : [ ]
  } ]
}