{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "Collections.emptyMap() is never null, so the returned result is guaranteed non-null.",
    "meta" : [ ]
  }, {
    "expression" : "result.isEmpty()",
    "rationale" : "Collections.emptyMap() returns an empty map, so the result must always be empty.",
    "meta" : [ ]
  } ]
}