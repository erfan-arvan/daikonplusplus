{
  "invariants" : [ {
    "expression" : "result.size() == records.size()",
    "rationale" : "Sorting does not add or remove elements, it only reorders them.",
    "meta" : [ ]
  }, {
    "expression" : "result.stream().allMatch(r -> records.contains(r))",
    "rationale" : "All elements in the result must have originated from the input list.",
    "meta" : [ ]
  }, {
    "expression" : "records.stream().allMatch(r -> result.contains(r))",
    "rationale" : "All input records must be present in the result, though in a different order.",
    "meta" : [ ]
  }, {
    "expression" : "IntStream.range(0, result.size() - 1).allMatch(i -> getComparableSortColumns(result.get(i)).compareTo(getComparableSortColumns(result.get(i + 1))) <= 0)",
    "rationale" : "The result list must be sorted according to the comparison.",
    "meta" : [ ]
  }, {
    "expression" : "outputPartitions >= 0",
    "rationale" : "Partition count should be non-negative.",
    "meta" : [ ]
  } ]
}