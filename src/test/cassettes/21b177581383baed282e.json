{
  "invariants" : [ {
    "expression" : "actor == null || actor instanceof com.badlogic.gdx.scenes.scene2d.Actor",
    "rationale" : "The generic type T is bounded by Actor, so if actor is not null it must be an Actor.",
    "meta" : [ ]
  } ]
}