{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "The method always throws an exception and never returns normally, so only invariants that are always true hold at this program point.",
    "meta" : [ ]
  } ]
}