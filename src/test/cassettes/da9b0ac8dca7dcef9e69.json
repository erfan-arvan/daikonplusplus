{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "This branch always throws, so no other invariant about the inputs or object state can be guaranteed to hold at a successful exit; the only reachable program point before returning is the exception throw; thus, only `true` is universally invariant.",
    "meta" : [ ]
  } ]
}