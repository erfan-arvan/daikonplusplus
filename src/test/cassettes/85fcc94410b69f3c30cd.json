{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method returns the field padTop, which is presumably never null (as a UI padding value), so the result is not null.",
    "meta" : [ ]
  } ]
}