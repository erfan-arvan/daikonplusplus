{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "Since the method always throws an exception, no value is returned, so result is never assigned and defaults to null at exit.",
    "meta" : [ ]
  } ]
}