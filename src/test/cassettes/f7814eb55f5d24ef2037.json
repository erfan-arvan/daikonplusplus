{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "context.mapToPair should return a non-null object.",
    "meta" : [ ]
  }, {
    "expression" : "result.keySet().containsAll(writeResult.getWriteStatuses().stream().map(status -> status.getStat().getPartitionPath()).distinct().collect(java.util.stream.Collectors.toList()))",
    "rationale" : "The map's keys are exactly the partition paths collected from writeResult.",
    "meta" : [ ]
  }, {
    "expression" : "result.values().stream().allMatch(list -> list != null)",
    "rationale" : "Each list of file ids for a partition should be non-null.",
    "meta" : [ ]
  }, {
    "expression" : "result.size() <= writeResult.getWriteStatuses().size()",
    "rationale" : "Number of partitions (keys) should not exceed the number of write statuses.",
    "meta" : [ ]
  }, {
    "expression" : "result.keySet().stream().allMatch(partition -> result.get(partition).equals(getAllExistingFileIds(partition)))",
    "rationale" : "For every partition, the value is the result of getAllExistingFileIds for that partition.",
    "meta" : [ ]
  } ]
}