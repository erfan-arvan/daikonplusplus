{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method always allocates an Array<AssetDescriptor> and returns it, so result is never null.",
    "meta" : [ ]
  }, {
    "expression" : "result.size >= 0",
    "rationale" : "Array.size in libGDX is always non-negative, reflecting the number of contained elements; empty arrays have size 0.",
    "meta" : [ ]
  }, {
    "expression" : "result.size <= 1",
    "rationale" : "At most one item is added to result (if atlasFileHandle != null), so result cannot have more than 1 element.",
    "meta" : [ ]
  } ]
}