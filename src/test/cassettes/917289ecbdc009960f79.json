{
  "invariants" : [ {
    "expression" : "result == this.maxLength",
    "rationale" : "The method directly returns this.maxLength, so the returned value must equal this.maxLength.",
    "meta" : [ ]
  } ]
}