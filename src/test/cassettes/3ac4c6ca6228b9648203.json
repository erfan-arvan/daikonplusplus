{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "No parameters or object state are visible or in-scope, so the only invariant we can state is that method entry is always reachable (tautology avoided if possible, but here no variables exist).",
    "meta" : [ ]
  } ]
}