{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "The method never returns null; it returns either \"default\" string or the original non-null, non-empty value.",
    "meta" : [ {
      "key" : "nullness",
      "value" : "result is never null"
    } ]
  }, {
    "expression" : "!result.isEmpty()",
    "rationale" : "Result is either \"default\" or the original non-empty value, so it cannot be empty.",
    "meta" : [ {
      "key" : "emptiness",
      "value" : "result is never empty"
    } ]
  }, {
    "expression" : "value == null || value.isEmpty() ? result.equals(\"default\") : result.equals(value)",
    "rationale" : "Reflects the exact return logic of the method.",
    "meta" : [ {
      "key" : "conditional",
      "value" : "result equals \"default\" if input is null or empty; else equals input"
    } ]
  } ]
}