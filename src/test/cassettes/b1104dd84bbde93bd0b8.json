{
  "invariants" : [ {
    "expression" : "result == bucketInfoMap.get(bucketNumber)",
    "rationale" : "Because the method directly returns bucketInfoMap.get(bucketNumber), the result must always equal this value.",
    "meta" : [ ]
  } ]
}