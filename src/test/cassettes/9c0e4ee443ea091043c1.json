{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "Since the method throws an exception on every code path, it never actually returns a value, so 'result' is always null at the program point just before return.",
    "meta" : [ ]
  } ]
}