{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "No parameters or fields are explicitly in scope, so the only certain invariant is that execution is valid at method entry.",
    "meta" : [ ]
  } ]
}