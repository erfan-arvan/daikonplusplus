{
  "invariants" : [ {
    "expression" : "columnsToIndex != null",
    "rationale" : "Checking that the input parameter is not null before method body executes.",
    "meta" : [ ]
  }, {
    "expression" : "columnsToIndex == null || columnsToIndex.isEmpty() || columnsToIndex.get(0) != null",
    "rationale" : "If the list is non-null and non-empty, its first element should be non-null (defensive check).",
    "meta" : [ ]
  }, {
    "expression" : "columnsToIndex == null || columnsToIndex.size() >= 0",
    "rationale" : "If the list is non-null, its size is always non-negative as per List contract.",
    "meta" : [ ]
  } ]
}