{
  "invariants" : [ {
    "expression" : "result.isEmpty()",
    "rationale" : "Since the method always throws an exception, it never returns a value, so result must be empty.",
    "meta" : [ {
      "key" : "reason",
      "value" : "Method throws exception, never returns a non-empty Option"
    } ]
  } ]
}