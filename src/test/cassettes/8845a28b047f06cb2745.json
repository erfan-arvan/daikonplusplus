{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "No in-scope variables are available at method entry; only the implicit object state is accessible, but no fields or method parameters are listed or usable without explicit names.",
    "meta" : [ ]
  } ]
}