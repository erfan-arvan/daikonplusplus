{
  "invariants" : [ {
    "expression" : "args != null",
    "rationale" : "The args parameter is provided by the JVM and is guaranteed non-null on entry, so it should still be non-null at method exit.",
    "meta" : [ ]
  }, {
    "expression" : "args.length >= 0",
    "rationale" : "The length of an array is always non-negative, so this holds for the program argument array.",
    "meta" : [ ]
  } ]
}