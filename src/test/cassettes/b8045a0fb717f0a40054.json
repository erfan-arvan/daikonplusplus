{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method body always throws an exception and never reaches a return statement, so 'result' must be null at exit.",
    "meta" : [ ]
  } ]
}