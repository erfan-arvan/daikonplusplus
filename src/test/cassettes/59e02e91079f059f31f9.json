{
  "invariants" : [ {
    "expression" : "this.fadeScrollBars == fadeScrollBars",
    "rationale" : "The method assigns this.fadeScrollBars = fadeScrollBars unless already equal, so at method exit they are always equal.",
    "meta" : [ ]
  } ]
}