{
  "invariants" : [ {
    "expression" : "true",
    "rationale" : "Since 'result' is directly set to 'fadeScrollBars', and since the method returns that field, the only thing we can assert is that 'result' will always be a boolean, so this is a trivially true invariant.",
    "meta" : [ ]
  } ]
}