{
  "invariants" : [ {
    "expression" : "result >= 0",
    "rationale" : "Layer collections cannot have a negative size, so the count must be non-negative.",
    "meta" : [ ]
  }, {
    "expression" : "result == layers.size",
    "rationale" : "The method returns the size field of layers directly, so they must always be equal at method exit.",
    "meta" : [ ]
  } ]
}