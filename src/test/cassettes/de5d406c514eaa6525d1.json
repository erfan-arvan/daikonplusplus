{
  "invariants" : [ {
    "expression" : "blendMode != null",
    "rationale" : "Returning blendMode as a value suggests it is non-null in typical use; otherwise, a null return may need special handling.",
    "meta" : [ ]
  }, {
    "expression" : "true",
    "rationale" : "No in-scope variables or parameters are directly available at method entry, so the only generally valid invariant is that execution has reached this point.",
    "meta" : [ ]
  } ]
}