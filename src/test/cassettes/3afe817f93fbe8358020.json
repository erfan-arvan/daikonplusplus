{
  "invariants" : [ {
    "expression" : "result != null",
    "rationale" : "Collections.emptyList() is never null, so the returned list is always non-null.",
    "meta" : [ ]
  }, {
    "expression" : "result.isEmpty()",
    "rationale" : "The method always returns Collections.emptyList(), which is empty.",
    "meta" : [ ]
  }, {
    "expression" : "result.size() == 0",
    "rationale" : "The only possible return value is an empty list whose size is 0.",
    "meta" : [ ]
  } ]
}