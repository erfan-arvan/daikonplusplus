{
  "invariants" : [ {
    "expression" : "getStage() == null",
    "rationale" : "If 'return' is being executed before calling 'inputListener.keyDown', it is only possible via the 'if (getStage() == null)' branch.",
    "meta" : [ ]
  } ]
}