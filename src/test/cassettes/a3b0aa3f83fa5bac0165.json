{
  "invariants" : [ {
    "expression" : "layer != null",
    "rationale" : "Layer is dereferenced multiple times in the method, so it must not be null at exit.",
    "meta" : [ ]
  }, {
    "expression" : "layer.getWidth() >= 0",
    "rationale" : "Layer width is used in Math.min and as a loop guard. Negative values would not make logical sense and could cause errors.",
    "meta" : [ ]
  }, {
    "expression" : "layer.getHeight() >= 0",
    "rationale" : "Layer height is used in Math.min and as a loop guard. Negative values would not make logical sense and could cause errors.",
    "meta" : [ ]
  }, {
    "expression" : "layer.getTileWidth() > 0",
    "rationale" : "Used as a multiplication factor and division denominator in computing tile sizes and loop bounds.",
    "meta" : [ ]
  }, {
    "expression" : "layer.getTileHeight() > 0",
    "rationale" : "Used as a multiplication factor and division denominator in computing tile sizes and loop bounds.",
    "meta" : [ ]
  } ]
}