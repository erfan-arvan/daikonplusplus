{
  "invariants" : [ {
    "expression" : "result == null",
    "rationale" : "The method always throws an exception, so it never actually returns a value; if control escapes the method, the return value would be null.",
    "meta" : [ ]
  } ]
}