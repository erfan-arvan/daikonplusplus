{
  "invariants" : [ {
    "expression" : "result == HoodieRecord.HoodieRecordType.AVRO",
    "rationale" : "The method always returns HoodieRecord.HoodieRecordType.AVRO, so result will always be this value at method exit.",
    "meta" : [ ]
  } ]
}