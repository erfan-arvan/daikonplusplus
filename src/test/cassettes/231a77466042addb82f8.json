{
  "invariants" : [ {
    "expression" : "items.size != 0",
    "rationale" : "The method returns immediately if items.size == 0, so at method exit items.size must not be 0.",
    "meta" : [ ]
  } ]
}